{
  "buildCommand": "npm run build:all",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install --prefix frontend && npm install --prefix sanity",
  "rewrites": [
    {
      "source": "/sanity/:path*",
      "destination": "/sanity/index.html"
    }
  ]
}
