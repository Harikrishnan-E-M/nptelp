# Sanity Configuration (pre-configured)
VITE_SANITY_PROJECT_ID=1asbko6r
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-30

# Backend API (for CSV import + statistics)
VITE_API_URL=http://localhost:5000

# Optional: Add if using backend (currently not needed)
# SANITY_TOKEN=your_token_here
